/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Scala application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/6.9.1/userguide/building_java_projects.html
 */
plugins {
    id 'org.gradle.playframework' version '0.12'
}

play {
    platform {
        playVersion = '2.8.20'
        scalaVersion = '2.12.18'
        javaVersion = JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation 'org.scala-lang:scala-library:2.12.10'
    implementation 'com.typesafe.play:play-guice_2.12:2.8.0'
    testImplementation group: 'org.scalatestplus.play', name: 'scalatestplus-play_2.12', version: '5.1.0'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
    maven {
        name "lightbend-maven-releases"
        url "https://repo.lightbend.com/lightbend/maven-release"
    }
    ivy {
        name "lightbend-ivy-release"
        url "https://repo.lightbend.com/lightbend/ivy-releases"
        layout "ivy"
    }
    maven { url "https://plugins.gradle.org/m2/" }
}



/*
plugins {
    id 'play'
}

repositories {
    mavenCentral()
}

dependencies {
    play "com.typesafe.play:play_2.12:2.8.20"
    play "com.typesafe.play:play-java_2.12:2.8.20"
}

tasks.withType(JavaExec) {
    if (System.getProperty("javaCommand") == null) {
        if (System.getProperty("java.home")) {
            executable = "${System.getProperty("java.home")}/bin/java"
        } else {
            executable = "java"
        }
    }
}


*/